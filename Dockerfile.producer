FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY producer.py .

# 關閉 Python 的緩衝區，確保 log 即時輸出到 docker logs
ENV PYTHONUNBUFFERED=1

CMD ["python", "producer.py"]